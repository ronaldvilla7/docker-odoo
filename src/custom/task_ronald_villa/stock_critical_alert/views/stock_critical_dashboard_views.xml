<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action for the Critical Stock Dashboard -->
    <record id="action_stock_critical_dashboard" model="ir.actions.act_window">
        <field name="name">Tablero de Critical Stock</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="domain">[('is_critical_stock', '=', True)]</field>
        <field name="context">{'search_default_categ_id': 1}</field>
    </record>

    <!-- Custom search view to add the filter and group by -->
    <record id="view_product_template_search_critical" model="ir.ui.view">
        <field name="name">product.template.search.critical</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <filter string="In Critical Stock" name="critical_stock" domain="[('is_critical_stock', '=', True)]"/>
                <group expand="0" string="Group By">
                    <filter string="Category" name="group_by_categ_id" context="{'group_by': 'categ_id'}"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Menu in Inventory root-->
    <menuitem id="menu_stock_critical_dashboard"
              name="Critical Stock"
              parent="stock.menu_stock_root"
              action="action_stock_critical_dashboard"
              sequence="100"/>
</odoo>
